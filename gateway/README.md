# Gateway

Use LiteLLM (or Portkey equivalent) as single provider entrypoint.

## Capabilities
- Unified model routing
- Retry + fallback policy
- Spend tracking via callbacks

## Suggested run
- `litellm --config gateway/litellm-config.yaml --port 4000`
